import React from "react";
import { leftImage } from "../utils/imageUtils";

const Footer = () => {
  return (
    <div style={{ display: "flex", justifyContent: "start" }}>
      <img src={leftImage} />
    </div>
  );
};

export default Footer;
